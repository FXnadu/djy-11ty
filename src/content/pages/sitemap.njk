---
permalink: /sitemap.xml
eleventyExcludeFromCollections: true
---
<?xml version="1.0" encoding="UTF-8"?>
{#- 列表页 lastmod 使用最新文章日期，而非 "now"，避免虚假更新信号 -#}
{%- if collections.posts | length -%}
  {%- set latestPostDate = collections.posts[0].date | dateISO -%}
{%- else -%}
  {%- set latestPostDate = "now" | dateISO -%}
{%- endif -%}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>{{ siteConfig.url }}/</loc>
    <lastmod>{{ "now" | dateISO }}</lastmod>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>
  <url>
    <loc>{{ siteConfig.url }}/writing/</loc>
    <lastmod>{{ latestPostDate }}</lastmod>
    <changefreq>weekly</changefreq>
    <priority>0.9</priority>
  </url>
  <url>
    <loc>{{ siteConfig.url }}/writing/gene/</loc>
    <lastmod>{{ latestPostDate }}</lastmod>
    <changefreq>weekly</changefreq>
    <priority>0.7</priority>
  </url>
  <url>
    <loc>{{ siteConfig.url }}/shutter/</loc>
    <lastmod>{{ "now" | dateISO }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.7</priority>
  </url>
  {% for post in collections.posts %}
  <url>
    <loc>{{ siteConfig.url }}{{ post.url }}</loc>
    <lastmod>{{ post.date | dateISO }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.6</priority>
  </url>
  {% endfor %}
  {% for pageData in collections.tagPages %}
  {% if pageData.page == 0 %}
  <url>
    <loc>{{ siteConfig.url }}/writing/gene/{{ pageData.tag | tagSlug }}/</loc>
    <lastmod>{{ pageData.posts[0].date | dateISO }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.5</priority>
  </url>
  {% endif %}
  {% endfor %}
  {% for pageData in collections.yearPages %}
  {% if pageData.page == 0 %}
  <url>
    <loc>{{ siteConfig.url }}/writing/gene/{{ pageData.year }}/</loc>
    <lastmod>{{ pageData.posts[0].date | dateISO }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.5</priority>
  </url>
  {% endif %}
  {% endfor %}
</urlset>
